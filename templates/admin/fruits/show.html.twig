{% extends '../../base.html.twig' %}

{% block title %}Fruits{% endblock %}

{% block body %}
    <h1>Fruits</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fruit.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ fruit.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ fruit.prix }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ fruit.quantite }}</td>
            </tr>
            <tr>
                <th>Illustration</th>
                <td>{{ fruit.illustration }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_fruits_index') }}">back to list</a>

    <a href="{{ path('app_fruits_edit', {'id': fruit.id}) }}">edit</a>

    {{ include('fruits/_delete_form.html.twig') }}
{% endblock %}
