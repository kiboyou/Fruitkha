{% extends '../../base.html.twig' %}

{% block title %}PanierItem{% endblock %}

{% block body %}
    <h1>PanierItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ panier_item.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ panier_item.quantite }}</td>
            </tr>
            <tr>
                <th>Prix_sous_total</th>
                <td>{{ panier_item.prixSousTotal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_panier_item_index') }}">back to list</a>

    <a href="{{ path('app_panier_item_edit', {'id': panier_item.id}) }}">edit</a>

    {{ include('panier_item/_delete_form.html.twig') }}
{% endblock %}
